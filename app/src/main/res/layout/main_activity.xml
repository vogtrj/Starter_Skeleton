<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/myNavHostFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:navGraph="@navigation/nav_graph"
        app:defaultNavHost="true" />

    <!--    The FragmentContainerView replaced the "fragment as a child of LinearLayout" XML below.-->
    <!--    This was recommended by the Git commit and makes sense in the context of this layout, but-->
    <!--    with a more complex layout it might make sense to go back to a "fragment inside layout" structure.-->
    <!--    <LinearLayout-->
    <!--        android:layout_width="match_parent"-->
    <!--        android:layout_height="match_parent"-->
    <!--        android:orientation="vertical">-->
    <!--            <fragment-->
    <!--                android:id="@+id/myNavHostFragment"-->
    <!--                android:name="androidx.navigation.fragment.NavHostFragment"-->
    <!--                android:layout_width="match_parent"-->
    <!--                android:layout_height="match_parent"-->
    <!--                app:navGraph="@navigation/nav_graph"-->
    <!--                app:defaultNavHost="true" />-->
    <!--    </LinearLayout>-->
</FrameLayout>
